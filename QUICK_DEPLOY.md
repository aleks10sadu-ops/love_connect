# ⚡ Быстрый деплой за 10 минут

## 🎯 Цель
Задеплоить приложение так, чтобы вы и ваша жена могли подключаться через браузер.

## 📝 План
1. **Railway** - Socket.io сервер (5 мин)
2. **Vercel** - Сайт (5 мин)

---

## Шаг 1: Railway (Socket.io сервер)

### 1.1 Загрузите код на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/love-connect.git
git push -u origin main
```

### 1.2 Деплой на Railway
1. Откройте [railway.app](https://railway.app)
2. Войдите через GitHub
3. **New Project** → **Deploy from GitHub repo**
4. Выберите `love-connect`
5. Railway автоматически задеплоит
6. Дождитесь деплоя (2-3 минуты)
7. Скопируйте URL (например: `https://love-connect-production.up.railway.app`)

### 1.3 Настройка переменных
1. В проекте Railway → **Variables**
2. Добавьте:
   - `NODE_ENV` = `production`
3. Сохраните

---

## Шаг 2: Vercel (Сайт)

### 2.1 Деплой через Dashboard
1. Откройте [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. **Add New...** → **Project**
4. Импортируйте `love-connect`
5. **ВАЖНО**: Перед деплоем:
   - Откройте **Environment Variables**
   - Добавьте:
     - **Name**: `NEXT_PUBLIC_SOCKET_URL`
     - **Value**: URL из Railway (шаг 1.2)
   - **Save**
6. Нажмите **Deploy**
7. Дождитесь деплоя
8. Скопируйте URL (например: `https://love-connect.vercel.app`)

### 2.2 Обновите Railway
1. Вернитесь в Railway
2. **Variables** → Добавьте:
   - `FRONTEND_URL` = URL из Vercel (шаг 2.1)
3. Railway автоматически передеплоит

---

## ✅ Готово!

1. Откройте сайт на Vercel
2. Введите имя и ID комнаты
3. Поделитесь ID комнаты с женой
4. Она открывает тот же сайт и вводит тот же ID
5. Наслаждайтесь видеозвонком! 💕

---

## 🔧 Если что-то не работает

### Проверьте:
1. Оба URL работают (откройте в браузере)
2. `NEXT_PUBLIC_SOCKET_URL` в Vercel правильный
3. `FRONTEND_URL` в Railway правильный
4. Оба используют HTTPS

### Консоль браузера (F12):
- Если видите ошибки CORS - проверьте `FRONTEND_URL` в Railway
- Если не подключается - проверьте `NEXT_PUBLIC_SOCKET_URL` в Vercel

