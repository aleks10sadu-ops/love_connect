# 🚀 Пошаговая инструкция по деплою на Vercel

## 📋 Что нужно сделать

Для работы приложения нужно развернуть **2 части**:
1. **Socket.io сервер** (на Railway/Render) - для WebSocket соединений
2. **Next.js приложение** (на Vercel) - основной сайт

---

## ⚠️ Важно: Два варианта деплоя

### Вариант A: Простой (Рекомендуется для начала)
Используйте `server.js` - он включает и Next.js и Socket.io вместе. Задеплойте на Railway.

### Вариант B: Раздельный (Для продакшена)
- Socket.io сервер (`socket-server.js`) на Railway
- Next.js приложение на Vercel

---

## Шаг 1: Развертывание Socket.io сервера на Railway (Рекомендуется)

Railway - самый простой вариант, бесплатный тариф.

### 1.1 Подготовка репозитория

1. Создайте аккаунт на [GitHub](https://github.com) (если нет)
2. Создайте новый репозиторий на GitHub
3. Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/love-connect.git
   git push -u origin main
   ```

### 1.2 Деплой на Railway

**Вариант A: Используя server.js (проще)**

1. Зарегистрируйтесь на [Railway](https://railway.app) (можно через GitHub)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `love-connect`
5. Railway автоматически определит Node.js проект
6. Нажмите на проект → **Settings** → **Variables**
7. Добавьте переменные окружения:
   - `NODE_ENV` = `production`
   - `FRONTEND_URL` = `https://your-app.vercel.app` (добавите после деплоя на Vercel)
8. Railway автоматически задеплоит проект
9. После деплоя Railway предоставит URL (например: `https://your-app.railway.app`)
10. **Скопируйте этот URL** - он понадобится для Vercel

**Вариант B: Только Socket.io сервер (socket-server.js)**

1. Создайте отдельную папку или используйте `socket-server.js`
2. На Railway создайте новый проект
3. В настройках проекта укажите:
   - **Root Directory**: оставьте пустым (или создайте подпапку)
   - **Start Command**: `node socket-server.js`
4. Добавьте переменные окружения:
   - `FRONTEND_URL` = `https://your-app.vercel.app`
5. Railway задеплоит только Socket.io сервер

### 1.3 Проверка Socket.io сервера

Откройте в браузере: `https://your-app.railway.app` - должна открыться ваша Next.js страница.

---

## Шаг 2: Деплой Next.js приложения на Vercel

### 2.1 Подготовка через Vercel Dashboard (Проще)

1. Зарегистрируйтесь на [Vercel](https://vercel.com) (можно через GitHub)
2. Нажмите **"Add New..."** → **"Project"**
3. Импортируйте ваш GitHub репозиторий `love-connect`
4. Vercel автоматически определит Next.js проект
5. **ВАЖНО**: Перед деплоем добавьте переменную окружения:
   - Нажмите **"Environment Variables"**
   - Добавьте:
     - **Name**: `NEXT_PUBLIC_SOCKET_URL`
     - **Value**: URL вашего Railway сервера (например: `https://your-app.railway.app`)
   - Нажмите **"Save"**
6. Нажмите **"Deploy"**
7. Дождитесь завершения деплоя
8. Vercel предоставит URL (например: `https://love-connect.vercel.app`)

### 2.2 Или через Vercel CLI

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой (в корне проекта)
vercel

# Добавьте переменную окружения
vercel env add NEXT_PUBLIC_SOCKET_URL production
# Введите URL вашего Railway сервера

# Передеплойте
vercel --prod
```

---

## Шаг 3: Настройка CORS на Socket.io сервере

Нужно разрешить подключения с вашего Vercel домена.

### Обновите server.js

Откройте `server.js` и обновите CORS настройки:

```javascript
const io = new Server(httpServer, {
  cors: {
    origin: [
      'https://your-app.vercel.app',  // Ваш Vercel URL
      'https://*.vercel.app',         // Все Vercel поддомены
      process.env.FRONTEND_URL || '*', // Или через переменную окружения
    ],
    methods: ['GET', 'POST'],
    credentials: true,
  },
})
```

Или добавьте переменную окружения на Railway:
- `FRONTEND_URL` = `https://your-app.vercel.app`

После изменения закоммитьте и запушьте в GitHub - Railway автоматически передеплоит.

---

## Шаг 4: Проверка работы

1. Откройте ваш сайт на Vercel: `https://your-app.vercel.app`
2. Введите ваше имя (например: "Я")
3. Используйте ID комнаты по умолчанию или создайте свой
4. Нажмите "Подключиться"
5. **На другом устройстве/браузере** откройте тот же сайт
6. Введите имя жены (например: "Жена")
7. Введите **тот же ID комнаты**
8. Нажмите "Подключиться"
9. Должно установиться соединение! 🎉

---

## 🔧 Альтернатива: Render вместо Railway

Если Railway не работает, используйте Render:

1. Зарегистрируйтесь на [Render](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `love-connect-socket`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free`
5. Добавьте переменные окружения:
   - `NODE_ENV` = `production`
6. Нажмите **"Create Web Service"**
7. Render предоставит URL (например: `https://love-connect-socket.onrender.com`)
8. Используйте этот URL в Vercel как `NEXT_PUBLIC_SOCKET_URL`

---

## ❗ Важные моменты

1. **HTTPS обязателен** - WebRTC не работает без HTTPS
2. **Оба сервера должны быть на HTTPS** - и Vercel, и Railway/Render
3. **CORS должен быть настроен** - иначе соединение не установится
4. **Переменная окружения** `NEXT_PUBLIC_SOCKET_URL` должна быть установлена в Vercel

---

## 🐛 Устранение проблем

### Соединение не устанавливается

1. Откройте консоль браузера (F12)
2. Проверьте ошибки в консоли
3. Убедитесь, что `NEXT_PUBLIC_SOCKET_URL` правильный
4. Проверьте, что Socket.io сервер доступен (откройте URL в браузере)

### CORS ошибки

1. Обновите CORS настройки в `server.js`
2. Добавьте ваш Vercel URL в список разрешенных
3. Передеплойте на Railway

### Видео не работает

1. Убедитесь, что используете HTTPS
2. Проверьте разрешения браузера на камеру/микрофон
3. Попробуйте другой браузер (Chrome/Edge работают лучше всего)

---

## 📱 Использование

После деплоя:

1. Вы и ваша жена открываете сайт на Vercel
2. Оба вводите **один и тот же ID комнаты**
3. Подключаетесь и наслаждаетесь видеозвонком! 💕

