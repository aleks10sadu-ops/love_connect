# 🚀 Инструкция по деплою

## Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер:
```bash
npm run dev
```

3. Откройте [http://localhost:3000](http://localhost:3000)

## Деплой на Vercel + Отдельный Socket.io сервер

### Шаг 1: Разверните Socket.io сервер

Выберите один из вариантов:

#### Вариант A: Railway (Рекомендуется - бесплатно)

1. Зарегистрируйтесь на [Railway](https://railway.app)
2. Создайте новый проект
3. Подключите ваш GitHub репозиторий
4. Railway автоматически определит Node.js проект
5. Добавьте переменную окружения:
   - `NODE_ENV=production`
6. Railway автоматически предоставит URL вашего сервера

#### Вариант B: Render (Бесплатно)

1. Зарегистрируйтесь на [Render](https://render.com)
2. Создайте новый "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - Build Command: `npm install`
   - Start Command: `npm start`
5. Render предоставит URL вашего сервера

#### Вариант C: Fly.io (Бесплатно)

1. Установите Fly CLI: `npm install -g @fly/cli`
2. Зарегистрируйтесь: `fly auth signup`
3. Создайте приложение: `fly launch`
4. Деплой: `fly deploy`

### Шаг 2: Деплой Next.js приложения на Vercel

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплой:
```bash
vercel
```

4. Добавьте переменную окружения в Vercel Dashboard:
   - Перейдите в Settings → Environment Variables
   - Добавьте: `NEXT_PUBLIC_SOCKET_URL` = URL вашего Socket.io сервера

5. Передеплойте:
```bash
vercel --prod
```

## Альтернатива: Использование готового сервиса

Если не хотите разворачивать отдельный сервер, можете использовать:

### Pusher (Бесплатный тариф до 200k сообщений/день)

1. Зарегистрируйтесь на [Pusher](https://pusher.com)
2. Создайте новый канал
3. Получите ключи API
4. Обновите код для использования Pusher вместо Socket.io

### Socket.io Cloud (Платный)

1. Зарегистрируйтесь на [Socket.io Cloud](https://www.socket.io/cloud)
2. Создайте проект
3. Получите URL и ключи
4. Обновите код

## Проверка работы

1. Откройте ваше приложение на Vercel
2. Введите имя и ID комнаты
3. Откройте в другом браузере/устройстве
4. Введите то же ID комнаты
5. Должно установиться соединение!

## Устранение проблем

### Соединение не устанавливается

- Проверьте, что Socket.io сервер запущен и доступен
- Проверьте переменную окружения `NEXT_PUBLIC_SOCKET_URL`
- Убедитесь, что CORS настроен правильно на Socket.io сервере
- Проверьте консоль браузера на ошибки

### Видео не работает

- Убедитесь, что вы дали разрешение на использование камеры/микрофона
- Проверьте, что используете HTTPS (обязательно для WebRTC)
- Проверьте настройки браузера

### Проблемы с WebRTC

- WebRTC требует HTTPS (кроме localhost)
- Некоторые сети блокируют STUN серверы
- Попробуйте использовать TURN сервер (для сложных сетей)

