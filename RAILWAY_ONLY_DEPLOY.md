# 🚂 Деплой только на Railway (без Vercel)

Простая инструкция для деплоя всего приложения на Railway.

## ✅ Преимущества

- **Один сервис** - всё в одном месте
- **Проще настройка** - не нужно настраивать CORS и переменные окружения между сервисами
- **Бесплатно** - Railway предоставляет бесплатный тариф
- **Меньше точек отказа** - один сервис вместо двух

## 🚀 Быстрый деплой

### Шаг 1: Загрузите код на GitHub

Если ещё не загрузили:

```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### Шаг 2: Деплой на Railway

1. Откройте [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `love_connect`
6. Railway автоматически определит Node.js проект и начнёт деплой

### Шаг 3: Настройка (опционально)

1. В проекте Railway → **Settings** → **Variables**
2. Можно добавить (но не обязательно):
   - `NODE_ENV` = `production` (Railway обычно устанавливает автоматически)

### Шаг 4: Получите URL

1. После деплоя Railway предоставит URL (например: `https://loveconnect-production.up.railway.app`)
2. Скопируйте этот URL
3. Откройте его в браузере - должно работать!

## 🎉 Готово!

Теперь вы и ваша жена можете:
1. Открыть сайт на Railway URL
2. Ввести имена и один ID комнаты
3. Наслаждаться видеозвонком!

## 🔧 Настройка домена (опционально)

Если хотите использовать свой домен:

1. В Railway → **Settings** → **Networking**
2. Нажмите **"Generate Domain"** или **"Add Custom Domain"**
3. Следуйте инструкциям

## 📝 Важные моменты

- **HTTPS обязателен** - Railway автоматически предоставляет HTTPS
- **WebRTC работает** - всё настроено автоматически
- **Socket.io работает** - клиент автоматически подключается к тому же домену

## 🐛 Если что-то не работает

1. Проверьте логи в Railway → **Deployments** → последний деплой
2. Убедитесь, что деплой завершился успешно
3. Проверьте, что URL открывается в браузере
4. Откройте консоль браузера (F12) для отладки

## 💡 Отличия от деплоя с Vercel

- Не нужно настраивать `NEXT_PUBLIC_SOCKET_URL` - клиент автоматически использует текущий домен
- Не нужно настраивать `FRONTEND_URL` - всё на одном домене
- Проще и быстрее настроить

